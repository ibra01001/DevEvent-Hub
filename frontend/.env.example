# API base URL used by the frontend to reach the Node/Express backend.
VITE_API_BASE=http://localhost:4000/api

# Default app theme used only when no saved theme exists yet.
VITE_DEFAULT_THEME=light

